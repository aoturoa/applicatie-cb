{{#if moreless}}
<div data-decorator="showmoreless" data-config='{"amount": "{{morelessAmount}}"}'>
  <ul class="list list--unstyled">
{{/if}}

{{#each options }}

  {{#if ../moreless}}
  <li>
  {{/if}}

  <div class="input-checkbox">
    <input {{{arguments}}} class="checkbox__input {{inputClass}}" {{disabled}} {{checked}} type="checkbox" id="option-{{ id }}" name="{{ name }}" {{ status }} value="{{#if isBoolean}}true{{else}}{{text}}{{/if}}"
    />
    <label class="checkbox__label" for="option-{{ id  }}">
      {{{ text }}}
    </label>
    {{#if hasLink}}<a href="{{linkHref}}">{{{linkLabel}}}</a>{{/if}}
  </div>

  {{#if moreless}}
  </li>
  {{/if}}

{{/each}}

{{#if moreless}}
  </ul>
</div>
{{/if}}

